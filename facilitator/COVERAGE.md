# Teaching coverage

This map records original workshop coverage after reviewing the supplied temporary course topics. Source files and source wording are excluded from release artifacts.

| Topic group | Live use or essential demonstration | Follow-up route |
| --- | --- | --- |
| Editors, interpreter, notebook, terminal | Setup: VS Code, Python, commands, Colab | Next: Jupyter and notebook state |
| Values, types, variables, naming, assignment, expressions, statements, comments | Greeting and scratch examples | Next: numeric built-ins |
| Strings, concatenation, input, conversion | Greeting, one question, `int`, `float`, `str`, `type` | Next: escapes, immutability, string methods |
| Arithmetic, precedence, comparisons, booleans | Question scratch check and results arithmetic | Next: `math`, absolute values, rounding, min/max |
| Lists, dictionaries, zero-based indexing, nesting, membership | Loops lesson and question bank | Next: methods, slicing, negative indexing, tuples, copying, operators |
| if/elif/else, while, for, range, nested iteration | Question validation, loops, `range(1,4)`, scoring | Next: range steps, continue, search with break |
| Functions, parameters, arguments, return, None, function calls | Functions and results | Next: defaults, keyword arguments, unreachable statements, nested calls, local/enclosing scope |
| Imports and modules | Presenter random sample and local questions module | Next: math/random practice |
| Traversal, accumulation, mapping/filtering | Scoring loops and score totals | Next: map/filter, list comprehensions, nested traversal |
| Formatting and sorting | Results, f-strings, named sort key, dense ranks | Next: field widths, `.format()` |
| Classes, instances, identity, state, behavior, initialization, methods, self | Outside live scope | Next: Task class and independent instances |
| Encapsulation, class versus instance attributes, name mangling | Outside live scope | Next: conventions and limits, not security claims |
| Inheritance, subclass/base class, overriding, superclass initialization | Outside live scope | Next: TimedTask, super(), describe override |
| Git and GitHub | Clone, checkout, personal branch, status, add, commit, recovery | Next: personal remote, push, pull |

Corrections preserved in original explanations: triple-quoted strings are not Python comments; `sum` takes an iterable; class attributes remain valid Python; name mangling is not a security boundary; inheritance uses method resolution rather than source copying. No learner input is passed to `eval`.

The earlier workshop slide reference was not supplied in this repository or the user's links. The requested IPO frame and prediction checks are implemented from the agreed plans; no claim is made that unavailable slides were audited.

## Cluster grounding

The supplied PM context identifies COMMS, OSG, MKT, OET, Events, eServs, and HR. The quiz paraphrases broad work themes: visual communication; project records and operations; partners and networks; project finances; event programs and logistics; IT solutions; and member relationships. Associations are original editorial choices for the quiz, not validated psychological measures.

Each of seven questions contains every cluster once across its four choices. Each question has one single-cluster answer and three two-cluster answers. Across the bank, every cluster has seven opportunities and exactly one single-cluster answer. Real response preferences may still favor some answers. All-a is a deliberate complete-tie fixture, not an expected population outcome.
